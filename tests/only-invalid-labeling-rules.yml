on:
  workflow_dispatch:

jobs:
  label_issues:
    runs-on: ubuntu-latest
    name: Manage Issue Labels
    steps:
      - uses: actions/checkout@main
        with:
          submodules: 'true'
      - uses: ./github-project-column-issue-labeler/
        id: manage-project-issue-labels
        name: Manage Project Issue Labels
        with:
          column_label_config: >
            [
              {
                "columnName": "Name",
                "labelingRules": []
              },
              {
                "columnName": "Name",
                "labelingRules": [
                  {
                  },
                  {
                    "action": "ADD"
                  },
                  {
                    "labels": ["a", "b", "c"]
                  },
                  {
                    "action": 3,
                    "labels": ["a", "b", "c"]
                  },
                  {
                    "action": "ADD",
                    "labels": {
                      "a": "a"
                    }
                  },
                  {
                    "action": "Unsupported Action",
                    "labels": ["a", "b", "c"]
                  }
                ]
              }
            ]
          token: ${{secrets.GITHUB_TOKEN}}

# Result:

# Validating Config
#   Checking column at index 0
#     Column configuration at index: 0 did not contain any valid labeling rules. Skipping column.
#   Checking column at index 1
#     Could not make valid labeling rule from value at index: 0
#       key "action" was not found in the object
#     Could not make valid labeling rule from value at index: 1
#       key "labels" was not found in the object
#     Could not make valid labeling rule from value at index: 2
#       key "action" was not found in the object
#     Could not make valid labeling rule from value at index: 3
#       Member "action" was found not to be a string
#     Could not make valid labeling rule from value at index: 4
#       Member "labels" was found not to be an array
#     Could not make valid labeling rule from value at index: 5
#       Labeling action "UNSUPPORTED ACTION" is not supported. Supported actions are: ["ADD","REMOVE","SET"]
#     Column configuration at index: 1 did not contain any valid labeling rules. Skipping column.
# Could not find any valid actions to perform from the configuration