{
  "accessToken": "token",
  "owner": "repo owner",
  "repo": "repo name",
  "columns": [
    {
      "name": "column name",
      "labelingRules": [
        {
          "action": "Remove",
          "labels": ["This should not appear"]
        },
        {
          "action": "Set",
          "labels": ["This should appear", "🛩", "alphabetically first"]
        },
        {
          "action": "Add",
          "labels": ["This should not appear"]
        }
      ]
    }
  ]
}