on:
  workflow_dispatch:

jobs:
  label_issues:
    runs-on: ubuntu-latest
    name: Manage Issue Labels
    steps:
      - uses: actions/checkout@main
        with:
          submodules: 'true'
      - uses: ./github-project-column-issue-labeler/
        id: manage-project-issue-labels
        name: Manage Project Issue Labels
        with:
          column_label_config: >
            [
              {},
              {
                "a": 3,
                "b": "b"
              },
              {
                "columnName": "String"
              },
              {
                "labelingRules": []
              },
              {
                "columnName": 3,
                "labelingRules": []
              },
              {
                "columnName": "Name",
                "labelingRules": 3
              }
            ]
          token: ${{secrets.GITHUB_TOKEN}}

# Result:

# Validating Config
#   Could not make valid column configuration from value at index: 0
#     key "columnName" was not found in the object
#   Could not make valid column configuration from value at index: 1
#     key "columnName" was not found in the object
#   Could not make valid column configuration from value at index: 2
#     Member "columnName" was found not to be a string
#   Could not make valid column configuration from value at index: 3
#     key "columnName" was not found in the object
#   Could not make valid column configuration from value at index: 4
#     Column name cannot be empty string
#   Could not make valid column configuration from value at index: 5
#     Member "columnName" was found not to be a string
# Could not find any valid actions to perform from the configuration